import React from 'react';

const Home: React.FC = () => (
  <div className="container">
    <h1>React Todo App - Training Project</h1>
    <p>Welcome to the React training project! Navigate through different tasks using the menu above.</p>
    <p>Each task demonstrates different React concepts and features.</p>
  </div>
);

export default Home; 